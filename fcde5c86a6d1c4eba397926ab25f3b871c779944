TODO: Build a small OpenAPI spec-merge tool in OCaml (use a YAML library, e.g. yaml/ocaml-yaml + cohttp-free JSON via ezjsonm). Use case: Twilio Messaging V1 (messaging.twilio.com) + Twilio API v2010 (api.twilio.com) must be merged into a single spec before the openapi-generator-cli ingests it, because the generator takes only one -i. openapi-merge-cli / @redocly/cli are npm-only and absent from nixpkgs. An OCaml tool fits the existing toolchain (moc, dune) and gives us hermetic, in-repo collision-handling (path prefix, tag rename, operationId rename per input spec). See conversation 2026-05-21.

TODO: Recast the Motoko client-generator as an out-of-tree plugin to the upstream openapi-generator-cli. Today this fork bundles the Motoko codegen into the all-in-one CLI build; instead, extract MotokoClientCodegen + Mustache templates into a standalone motoko-codegen.jar that declares its CodegenConfig via META-INF/services and depends on stock openapi-generator-core. Then integrate via a flake in this repo only: nixpkgs ships the stock openapi-generator-cli derivation, our flake builds motoko-codegen.jar and stitches both onto the classpath at invocation time. Benefits: no more building the whole CLI from source; faster regen; cleaner upstreaming path (the JAR is already its own thing). Open question: nixpkgs CLI wrapper's classpath-extension hook — investigate before committing to this layout. See conversation 2026-05-21.
