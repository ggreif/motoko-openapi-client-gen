{
  "_comment": "Inputs are resolved relative to this file's directory. Per-input transforms run before the merge so collisions can be disambiguated. Tag/schema/operationId prefixes also drive the resulting Motoko module names (e.g. tagPrefix=MessagingV1 yields MessagingV1ServiceApi.mo).",
  "inputs": [
    {
      "path": "external/twilio_messaging_v1.json"
    }
  ]
}
